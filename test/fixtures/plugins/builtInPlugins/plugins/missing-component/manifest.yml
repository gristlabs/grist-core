name: missing-components
version: 0.0.1
# missing `components` entry
contributions:
  fileParsers:
    - fileExtensions: ["csv"]
      parseFile:
        component: "safePython"
        name: "csv_parser"
