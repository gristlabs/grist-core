# When you update this, make sure to update worker/package.json and worker/yarn.lock,
# then run `make clean` and `make setup` to fetch the new pyodide versions.
export PYODIDE_VERSION=0.28.1
# Previous versions:
#   GRIST_PYODIDE_VERSION 3 was version 0.23.4
